# 🛰 OrbitalMind AI

> **AI-powered satellite conjunction screening** — предотвращение столкновений спутников с использованием машинного обучения и орбитальной механики.

---

## 📋 Описание проекта

OrbitalMind AI — веб-приложение для мониторинга потенциальных сближений спутников.  
Система анализирует один защищаемый объект против каталога объектов за горизонт 24 часа, используя два уровня анализа:

1. **Геометрический анализ** — двухпроходной алгоритм на основе SGP4 пропагатора
2. **ML предсказание** — RandomForest + GradientBoosting по орбитальным параметрам

---

## 🏗 Архитектура решения

```
┌─────────────────────────────────────────────────────────┐
│                    OrbitalMind AI                       │
├─────────────────┬───────────────────┬───────────────────┤
│   Data Layer    │  Processing Layer │   UI Layer        │
│                 │                   │                   │
│ • CelesTrak API │ • SGP4 (sgp4 lib) │ • Streamlit       │
│ • TLE файлы     │ • Two-pass scan   │ • Plotly 3D       │
│ • client_tle.txt│ • ML Pipeline     │ • Таблица рисков  │
│ • catalog_tle   │ • Orbit extractor │ • Метрики         │
└─────────────────┴───────────────────┴───────────────────┘
```

### Поток данных

```
TLE данные → parse_tle_text() → Satrec объекты
                                      │
                    ┌─────────────────┴──────────────────┐
                    │                                     │
         compute_min_distance_24h()            predict_risk_ml()
         (SGP4 двухпроходной скан)             (ML Pipeline)
                    │                                     │
              min_d, TCA, rel_v             ml_risk, Pc%, features
                    │                                     │
                    └─────────────────┬──────────────────┘
                                      │
                              DataFrame → UI
```

---

## 🤖 Алгоритмы и модели ИИ

### 1. SGP4 Пропагатор (Simplified General Perturbations)

Стандартный алгоритм для вычисления позиции и скорости спутника по TLE данным.  
Учитывает: атмосферное торможение, несферичность Земли, лунно-солнечные возмущения.

**Двухпроходной алгоритм поиска сближений:**
- **Pass 1 (grубый):** шаг 60с, 1440 точек за 24ч → определяет локальные минимумы расстояния
- **Pass 2 (точный):** шаг 5с, окно ±2 минуты вокруг каждого минимума → уточняет TCA

```python
# Обнаружение минимума: расстояние начало расти → предыдущая точка была минимумом
if d > prev_d:
    minima_t.append(prev_t)
```

### 2. RandomForestClassifier (scikit-learn)

| Параметр | Значение |
|----------|----------|
| Деревьев | 150 |
| max_depth | 8 |
| class_weight | balanced |
| Задача | HIGH / MED / LOW |

**Входные признаки:**

| Признак | Описание | Физический смысл |
|---------|----------|-----------------|
| `altitude_km` | Высота орбиты | Ниже LEO → плотнее трафик |
| `inclination_deg` | Наклонение | Полярные орбиты пересекают все плоскости |
| `eccentricity` | Эксцентриситет | Эллиптические орбиты = непредсказуемые перигеи |
| `mean_motion` | Среднее движение | Связано с орбитальным периодом (3-й закон Кеплера) |
| `bstar` | Баллистический коэф. | Торможение в атмосфере → непредсказуемость |

**Высота орбиты вычисляется из 3-го закона Кеплера:**
```python
semi_major_axis = (mu / (mean_motion_rad_s ** 2)) ** (1/3)
altitude_km = semi_major_axis - 6371.0  # радиус Земли
```

### 3. GradientBoostingRegressor (scikit-learn)

| Параметр | Значение |
|----------|----------|
| Итераций | 100 |
| learning_rate | 0.1 |
| max_depth | 4 |
| Задача | Pc% (вероятность столкновения 0–100%) |

**Обучение:** синтетические данные 2000 объектов с физически обоснованными закономерностями.

**Формула риска для генерации меток:**
```python
risk_score = (
    (1 - altitude / 2000) * 0.5      # ниже орбита → выше риск
    + (inclination / 98)  * 0.25     # полярные орбиты опаснее
    + eccentricity        * 3.0      # эллиптические орбиты
    + clip(bstar, 0, 0.01) * 20      # торможение
)
```

### 4. Pipeline (StandardScaler + модель)

Все признаки нормализуются перед подачей в модели:
```python
Pipeline([
    ("scaler", StandardScaler()),
    ("clf", RandomForestClassifier(...))
])
```

---

## 🛠 Стек технологий

| Компонент | Технология | Версия |
|-----------|-----------|--------|
| UI / Frontend | Streamlit | ≥1.35 |
| Орбитальная механика | sgp4 | ≥2.22 |
| ML классификация | scikit-learn RandomForest | ≥1.3 |
| ML регрессия | scikit-learn GradientBoosting | ≥1.3 |
| Визуализация 3D | Plotly | ≥5.18 |
| Данные | Pandas, NumPy | ≥2.0, ≥1.24 |
| TLE источник | CelesTrak API (HTTP) | — |
| HTTP клиент | requests | ≥2.31 |
| Язык | Python | 3.11 |

---

## 📁 Структура проекта

```
orbitalmind/
├── app.py              # Основное приложение
├── requirements.txt    # Зависимости
├── client_tle.txt      # TLE защищаемого спутника (опционально)
├── catalog_tle.txt     # Локальный каталог TLE (опционально)
├── data/               # Дополнительные данные
└── README.md           # Эта документация
```

---

## 🚀 Инструкция по запуску

### Требования
- Python 3.10+
- pip

### Установка

```bash
# 1. Клонировать репозиторий
git clone https://github.com/your-team/orbitalmind-ai.git
cd orbitalmind-ai

# 2. Установить зависимости
pip install -r requirements.txt

# 3. Запустить приложение
python -m streamlit run app.py
```

Приложение откроется по адресу: **http://localhost:8501**

### Быстрый тест (без настройки)

1. В боковой панели выбери **"First from CelesTrak group"** → Bootstrap group: `stations`
2. Catalog source: **CelesTrak group** → `starlink`
3. Max catalog objects: **50** (для быстрого теста)
4. Нажми **🚀 Run Analysis**

---

## 🎮 Описание интерфейса

### Боковая панель (Settings)

| Настройка | Описание |
|-----------|----------|
| Client Satellite | Источник защищаемого спутника: ручной TLE / CelesTrak / файл |
| Catalog Source | Источник каталога: CelesTrak группа или локальный файл |
| Max catalog objects | Сколько объектов проверять (10–2000) |
| Coarse step | Шаг грубого сканирования в секундах |
| Fine step | Шаг точного уточнения в секундах |
| Risk Thresholds | Пороги HIGH/MED в километрах |
| ML Toggle | Включить/выключить ML предсказание |

### Результаты

| Колонка | Описание |
|---------|----------|
| Min Distance (km) | Минимальное расстояние за 24ч |
| TCA (UTC) | Время наибольшего сближения |
| Rel. Velocity (km/s) | Относительная скорость при TCA |
| Risk (geometry) | Риск по дистанции: HIGH/MED/LOW |
| Risk (ML) | Риск по ML модели |
| Pc % (ML) | Вероятность столкновения в % |
| Altitude (km) | Высота орбиты объекта |

---

## 📊 Критерии риска

| Уровень | Условие | Источник |
|---------|---------|---------|
| HIGH | miss distance < 10 км | US Space Fence screening volume (~5km radial) |
| MED | miss distance < 50 км | Оперативная практика мониторинга |
| LOW | miss distance ≥ 50 км | — |

---

## ⚠️ Ограничения MVP

- ML модель обучена на **синтетических данных** — для продакшна требуется обучение на исторических конъюнкциях NASA/ESA
- Pc% является **условной оценкой**, а не истинной вероятностью столкновения (для точного Pc нужна ковариационная матрица)
- Горизонт анализа — **24 часа** (в roadmap: 72ч и 7 суток)

---

## 📜 Лицензия

MIT License — свободное использование, модификация и распространение.

---



Если не откроеться напишите в тг @yernarre

Как запустить MVP OrbitalMind AI:

Проверьте содержимое папки orbitalmind:
Убедитесь, что в ней находятся все необходимые компоненты:

app.py — главное приложение (интерфейс и логика).

requirements.txt — список библиотек для установки.

catalog_tle.txt — база данных орбитальных параметров спутников.

client_tle.txt — параметры вашего спутника для проверки сближений.

data/ — папка с данными для обучения моделей машинного обучения (ML).

Запустите терминал и введите команды по очереди:

Шаг 1: Перейдите в рабочую папку

Bash
cd orbitalmind
(Эта команда заходит внутрь папки, где лежат все файлы кода).

Шаг 2: Установите зависимости

Bash
pip install -r requirements.txt
(Это установит библиотеки для ИИ и 3D-графики: sgp4, scikit-learn, plotly и др.).

Шаг 3: Запустите сайт

Bash
streamlit run app.py
Что жюри увидит после запуска:
В браузере откроется интерактивная панель. Программа прочитает файлы catalog_tle.txt и client_tle.txt, проведет симуляцию SGP4 и с помощью моделей RandomForest и GradientBoosting выдаст прогноз: вероятность столкновения (Pc%) и уровень риска (HIGH/MED/LOW).

*OrbitalMind AI — Космический хакатон 2026*
