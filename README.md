# 🛰️ OrbitalMind AI

**OrbitalMind AI** — это система предотвращения столкновений спутников, использующая ИИ для анализа рисков в реальном времени.

## 🚀 Основной файл проекта
[cite_start]Весь логический код MVP находится в файле: **[`app.py`](./orbitalmind/app.py)**.

## 🛠️ Функционал (на основе `app.py`):
* [cite_start]**SGP4 Симуляция**: Двухпроходной алгоритм поиска сближений[cite: 1].
* [cite_start]**Machine Learning**: Модели `RandomForestClassifier` и `GradientBoostingRegressor` для оценки Pc% (вероятности столкновения)[cite: 1, 12].
* [cite_start]**Интерфейс**: Построен на `Streamlit` с интерактивной 3D-визуализацией орбит[cite: 1, 12].

## 📦 Как запустить
1. Установите зависимости: `pip install -r requirements.txt`
2. Запустите MVP: `streamlit run orbitalmind/app.py`
    else:
        st.warning("Could not compute client satellite position for 3D plot (SGP4 error).")
